{
  "name": "gbrain-capture",
  "version": "0.1.0",
  "description": "Chrome extension to capture web content into GBrain",
  "private": true,
  "scripts": {
    "vendor": "cp node_modules/@mozilla/readability/Readability.js lib/readability.js",
    "setup": "./setup.sh",
    "serve": "bun run server.ts",
    "serve:bg": "bun run server.ts &",
    "test": "bun test"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "gbrain": "github:garrytan/gbrain"
  }
}
